@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Preferred Drink Of the Week</h2>
<h4>Just for you</h4>

<div class="row">
    @foreach(var drink in Model.PreferredDrinks)
    {
        @await Html.PartialAsync("DrinksSummary", drink)
    }

</div>